import styled from "styled-components"

const ProjectTag = styled.div`
  margin: 2px;
  font-size: 0.5rem;

  &:first-child {
    margin-left: 0;
  }

  &:last-child {
    margin-right: 0;
  }
`

export default ProjectTag
